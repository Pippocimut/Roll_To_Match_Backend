<ul id="campaigns-list" class="py-2 -my-2 overflow-x-auto sm:-mx-6 sm:px-6 lg:-mx-8 lg:px-8">
    <% campaigns.forEach(campaign=> { %>
        <%- include('campaign-card', {campaign: campaign}) %>
            <% }) %>
</ul>